html, body {
	background-color:#1d0d0d; /*#1d0d0d is the color of the background*/
	margin:0;
	padding:0;
	height:100%; /*this makes the html and body take up the whole screen, allowing us to push the footer to the bottom. No, they don't automatically do that, necessarily!*/
	z-index:-1;
}
.wrapper{ /*need a wrapper to get the footer element to stay down*/
	min-height:100%;
	position:relative;
}
.header{
    padding:10px;
    background-color:#1d0d0d;
    width:100%; 
}

.header li{
    display:inline;
	padding:10px;
}
.centeredmenu {
   float:left;
   width:100%;
   background: #fff;
   /*border-bottom:4px solid #000;*/
   overflow:hidden;
   position:relative;
}
.centeredmenu ul {
   clear:left;
   float:left;
   list-style:none;
   margin:0px;
   padding:0;
   position:relative;
   left:45%;
   text-align:center;
   font-family:arial;
}

.centeredmenu ul li {
   display:block;
   float:left;
   list-style:none;
   margin:0;
   padding:0;
   position:relative;
   right:50%;
}

.centeredmenu ul li a {
   display:block;
   margin:0 0 0 1px;
   padding:3px 20px;
   background:#fff;
   color:#1d0d0d;
   text-decoration:none;
   line-height:1.3em;
}
   
.centeredmenu ul li a:hover {
   background:#1d0d0d;
   color:#fff;
}

.centeredmenu ul li a.active,
.centeredmenu ul li a.active:hover {
   color:#fff;
   background:#1d0d0d;
   font-weight:bold;
}
.picbox{
    position:absolute;
    top:0%;
}
.picbox ul li{
    list-style-type:none;
    width:200px;
    min-height:200px;
    margin:5px;
}
.paybox{
    position:absolute;
    top:0px;
    left:230px;
}
.paybox ul li{
    list-style-type:none;
    width:200px;
    min-height:100px;
    margin:100px;
}
.main {
	margin-top:3%;
	position:relative;/*positioned this way so I can position the inside divs with absolute positioning*/
	z-index:0;
}
.nicebox {
    position:absolute;
    margin:20px;
    background-color:white;
    z-index: 1;
}
}
